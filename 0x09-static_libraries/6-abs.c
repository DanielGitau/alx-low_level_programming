#include "main.h"
/**
 *_abs - Calculate the absolute value of an integer
 *@n: integer to be calcualted
 *Return: absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
